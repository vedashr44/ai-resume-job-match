"""
Core package for the AI Resume-Job Match Recommender System.

This exposes helper functions so the rest of the project can import
short names such as `from app.recommender import JobRecommender`.
"""
